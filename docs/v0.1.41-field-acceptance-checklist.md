# v0.1.41 field acceptance checklist

- [ ] Record approved register filename, SHA-256, revision, import ID, and the
      frozen IP/BACnet contract digest.
- [ ] Record exact application commit, portable/image hash, run ID, and evidence-set ID.
- [ ] Run the owner-approved full cadence with independent append-only collector evidence.
- [ ] Require `window_completed=true` and `termination_reason=window_elapsed`.
- [ ] Keep Stopped, broker interruption, primary-cap, malformed, wrong-topic, unexpected, and diagnostic-truncation evidence distinct.
- [ ] Generate condensed client and technical reports; verify payload manifest hashes and redaction.
- [ ] Scan the sanitized acceptance bundle for credentials and prohibited identifiers.
- [ ] Record acceptance-owner sign-off.

All rows remain open until recorded in the private field evidence store.
