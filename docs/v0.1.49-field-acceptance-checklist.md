# v0.1.49 field acceptance checklist

- [ ] Record the approved site and scan authorization in the private record.
- [ ] Use the v0.1.49 unified portable EXE on a test machine. Builtin TCP is
      the default; use a locally installed Nmap only after explicit opt-in.
- [ ] Complete an IP dry run and its authorized sealed live run. Confirm a
      repeated equivalent request returns the canonical run and the exact
      v0.1.48 raw replay remains accepted.
- [ ] Confirm a preview terminal event cannot settle a same-ID live epoch or
      reveal preview BACnet points, comparison data, MQTT topics, reports, or
      downloads.
- [ ] For MQTT templates, leave Payload type blank and use `#` only in a topic
      filter. Keep corrected local files private unless the approved asset list
      says they ship.
- [ ] Confirm the portable visible version and `/api/v1/health` version both
      identify v0.1.49.
- [ ] Perform separately approved IP, BACnet, MQTT, and UDMI site tests and
      retain private evidence. No BACnet write feature is provided.
- [ ] Record exact application commit, portable or image hash, run ID, epoch,
      and evidence-set ID.
- [ ] Generate condensed client and technical reports; verify payload manifest
      hashes and redaction.
- [ ] Record acceptance-owner sign-off.

All rows remain open until recorded in the private field evidence store.
