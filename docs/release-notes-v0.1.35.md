# v0.1.35 - Evidence-led live asset console, Windows portable rebuild

This historical Windows rebuild carries the evidence-led live asset console
and the corrected report-list session creation path.

## Windows portable download

Download `Smart_Commissioning_App_Windows_Portable.zip`, extract it into a new
empty directory, and run `SmartCommissioningApp.exe`.

- Source commit: `{{COMMIT}}`
- EXE SHA-256: `{{EXE_SHA256}}`
- ZIP SHA-256: `{{ZIP_SHA256}}`

The ZIP was built from the annotated v0.1.35 tag, tested with the core and
backend suites, boot-smoked from paths containing spaces, and stamped as
v0.1.35.
