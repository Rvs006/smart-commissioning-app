# v0.1.39 UDMI reconciliation decision log

Date: 2026-08-06
Baseline: v0.1.37 engineering evidence
Status: v0.1.39 candidate, not accepted for controlled field validation

This log separates application changes from decisions that require an approved
register, field owner, collector evidence, or acceptance owner. No unresolved
decision has been filled with an assumption.

| ID | Decision / action | Candidate implementation or evidence | Owner / required evidence | Status |
|---|---|---|---|---|
| R1 | Build and run identity | Run and report models now retain evidence-set identity, renderer version, source run identity, scope, capture metadata, and explicit provenance fields. Register uploads now retain the source-file SHA-256; fields unavailable at run creation remain visibly null. | Release owner: record the exact portable bundle hash, machine reference, broker reference, import identity, and source commit for the next field run. | Open external evidence |
| R2 | Canonical register authority and asset count | Import and report paths preserve the register contract and expected totals. The software does not choose between 752 and 780. | Register owner: approve one register filename, revision, SHA-256, import ID, owner, and asset count. | Blocker: 752 vs 780 |
| R3 | Capture completion and longest cadence | Cancellation, backstop, broker interruption, disk failure, truncation, and completed-window states are represented separately. Gate B requires `window_completed=true` and `termination_reason=window_elapsed`. | Field owner: approve whether the controlled window is 10 or 24 hours, then provide paired application and independent collector evidence. | Blocker: cadence and paired run |
| R4 | Raw payload retention and portability | Technical ZIP exports now contain `raw_evidence/manifest.json` and `raw_evidence/records.jsonl`. Records retain topic, asset, payload type, timestamps, retained flag, size, hash, and redaction status. | Release owner: open a v0.1.39 ZIP on a clean machine and retrieve one payload tied to a finding. | Candidate implementation; manual proof open |
| R5 | Payload-type contract | Payload applicability is explicit in the imported contract. Blank applicability remains unresolved and does not create expected facets. Reports do not infer state, metadata, and pointset for every asset. | Register owner: approve the per-asset or per-system applicability matrix and confirm whether a fourth payload type exists. | Candidate implementation; matrix open |
| R6 | Pointset timestamp and cadence findings | Timestamp parsing, retained delivery, broker receive time, payload time, and cadence status are separate. Malformed timestamps do not become stale findings. | Publisher and field owners: classify the 444 baseline findings and approve the interval used by the controlled register. | Candidate implementation; field classification open |
| R7 | Expected-versus-observed mismatch highlighting | Fault rows carry expected value, observed value, mismatch flag, payload type, topic, and raw evidence reference. Report detail includes a mismatch column and the UI exposes the distinction. | Product owner: confirm the approved accessible treatment, then complete pointer and keyboard checks in the packaged UI. | Candidate implementation; manual UI proof open |
| R8 | Two report products and metric hierarchy | A client metrics product is selectable and is the API default. A technical product retains full register, fault, comparison, timestamp, cadence, and raw-evidence references. Both use the same evidence-set ID. | Stakeholder owner: approve client fields, technical fields, floor treatment, and the audience wording. | Candidate implementation; product approval open |
| R9 | Floor reporting | Floor is read only from an explicit optional register column. Room is never promoted to Floor. Missing floor data is reported as `not_provided`. | Register owner: supply an approved Floor column or floor map, or explicitly accept no floor reporting. | Candidate implementation; source data open |
| R10 | Frozen provenance and report-set identity | Report formats share a parent evidence-set ID. The technical ZIP includes a manifest and artifact integrity metadata; report snapshots retain renderer, scope, filter, source, capture, register, and count metadata. | Release owner: produce the final cross-format manifest for one approved field run and verify all artifact hashes. | Candidate implementation; field manifest open |
| R11 | Application quality versus field-data quality | Acceptance eligibility is false for incomplete or failed capture conditions. Report wording separates application execution, publisher findings, field-data quality, and release acceptance. | Acceptance owner: approve the sign-off wording after reviewing the controlled run. | Candidate implementation; sign-off open |
| R12 | Manual regression and handoff | Automated coverage is present for core validation, report generation, export, cancellation metadata, applicability, raw evidence, and cross-format identity. | Acceptance owner: execute the manual record, attach screenshots, run IDs, report IDs, and disposition of each finding. | Open manual validation |
| R13 | Deferred surfaces and release wording | IP Discovery, BACnet Discovery, MQTT Discovery, BACnet-to-MQTT, EDQ / Device Qualification, and whole-application production readiness remain outside this reconciliation. | Product owner: maintain separate follow-up plans and keep them out of the v0.1.39 UDMI acceptance statement. | Recorded as deferred |

## Current external blockers

1. The authoritative asset count is unresolved: the available engineering export shows 780 while the frozen acceptance baseline is 752.
2. The approved payload applicability matrix and any fourth payload type are unresolved.
3. The required controlled cadence is unresolved between 10 and 24 hours.
4. The supplied private one-minute Stop output and reconciled transcript were not available for direct verification in this workspace.
5. No paired v0.1.39 application and independent collector run has been executed, so Gate A and Gate B remain open.

## Decision rule

Until every blocker above has an owner and evidence reference, describe the
software as a v0.1.39 candidate. Do not describe the candidate as accepted for
controlled field validation.
